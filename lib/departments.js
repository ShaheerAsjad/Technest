// Store "departments": lets the new networking catalogue live next to the original consumer-tech products.
export const DEPARTMENTS = {
  networking: 'Networking & IT',
  consumer: 'Consumer Tech',
};
export const DEPARTMENT_KEYS = Object.keys(DEPARTMENTS);
